#include <stdio.h>
int main (){
    float p;
    int f;
    printf (" Digite o preco do produto: ");
    scanf ("%d", &p);
     f = p * 0.08;
        if (p > 1000);
            printf (" O preco final = %d", f);

        {
            /* code */
        }
        
    return 0;
}